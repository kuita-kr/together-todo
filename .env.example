# Copy to .env. Everything runs WITHOUT these (rule-based router + offline).
# Set USE_GEMINI=1 and add a free key to switch the brain to the LLM router.

USE_GEMINI=0
GOOGLE_API_KEY=          # free key from https://aistudio.google.com/apikey
